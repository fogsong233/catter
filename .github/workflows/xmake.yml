name: xmake

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/xmake.yml"
      - "src/**"
      - "tests/**"
      - "api/**"
      - "xmake.lua"

  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/xmake.yml"
      - "src/**"
      - "tests/**"
      - "api/**"
      - "xmake.lua"

jobs:
  build:
    strategy:
      matrix:
        os: [windows-2025, ubuntu-24.04, macos-15]
        node-version: ['22']

    concurrency:
      group: ${{ github.ref }}-${{ github.base_ref }}-${{ github.head_ref }}-${{ matrix.os }}
      cancel-in-progress: true

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
           node-version: ${{ matrix.node-version }}
           cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build js lib and test
        run: pnpm run build-js-test

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: 3.0.4
          actions-cache-folder: ".xmake-cache"
          actions-cache-key: ${{ matrix.os }}
          package-cache: true
          package-cache-key: ${{ matrix.os }}

      - name: Setup gcc-14
        if: matrix.os == 'ubuntu-24.04'
        run: |
          sudo apt update
          sudo apt install -y gcc-14 g++-14 libstdc++-14-dev
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
          sudo update-alternatives --set gcc /usr/bin/gcc-14
          sudo update-alternatives --set g++ /usr/bin/g++-14

      - name: Setup llvm
        if: matrix.os == 'macos-15'
        run: |
          brew install llvm@20

      - name: Xmake configure for macos
        if: matrix.os == 'macos-15'
        run: |
          xmake config --clean --yes --mode=release --toolchain=clang --sdk=/opt/homebrew/opt/llvm@20
          xmake --verbose --diagnosis

      - name: Xmake configure for linux, windows
        if: matrix.os != 'macos-15'
        run: |
          xmake config --clean --yes --mode=release
          xmake --verbose --diagnosis
